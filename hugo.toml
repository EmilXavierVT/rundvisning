baseURL = '/'
locale = 'da'
title = 'Ungdomsøen Rundtur'
disableKinds = ['taxonomy', 'term', 'RSS', 'sitemap', 'robotsTXT']

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[params]
description = 'Interaktiv guidet tur på Ungdomsøen.'
